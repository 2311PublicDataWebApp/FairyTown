<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.fairytown.ft.blacklist.store.BlacklistStore">
  
  <select id="selectBlacklist" resultType="UserVO">
	SELECT * FROM USER_TBL 
   	WHERE USER_ID = #{userId} AND USER_ST = 'B'
  </select>  
  
  
  </mapper>